'use server';

/**
 * Server Actions del proyecto BitacoraTCU.
 *
 * Las Server Actions de Next.js deben estar en archivos marcados con
 * 'use server' y solo pueden exportar funciones async.
 *
 * Ejemplo de uso:
 *
 *   import { submitActividadAction } from '@/actions';
 *   // Llamar directamente desde un Server Component o desde un
 *   // formulario con el atributo action={submitActividadAction}
 */

// ---------- Placeholder ----------
// Añade aquí las acciones del servidor a medida que se necesiten.
// export async function submitActividadAction(formData: FormData) { ... }
